#!/usr/bin/env bash
source d-var.conf

sleepfor=$(printf "0\n1\n2\n3\n4\n5" | $D_MENU -p "Sleep for:  ")

password=$(cliphist list -max-items 10 | $D_MENU -p 'password: ' | cliphist decode)

sleep "${sleepfor}" && wtype "${password}"
